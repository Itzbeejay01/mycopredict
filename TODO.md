# TODO - Git cleanup: keep only MainWork

- [ ] Gather final confirmation of current branch state
- [ ] Checkout `MainWork`
- [ ] Delete all other local branches (`main`, `replit-agent`)
- [ ] Remove git remotes (`origin`, `gitsafe-backup`)
- [ ] Delete all remote-tracking references under `refs/remotes/*`
- [ ] Verify only `MainWork` remains under `git branch --all`
- [x] Record final `git remote -v` and branch list
